# 招聘数据NLP分析项目 - Cursor规则

## 项目概述
本项目分析广东省三大招聘网站（智联招聘、猎聘网、前程无忧）2022-2025年的招聘数据，总数据量约5GB。

## 核心目标
1. 对招聘市场进行深度NLP分析
2. 挖掘岗位需求趋势和技能要求
3. 分析薪资与技能、学历、经验的关系
4. 识别行业热点和就业机会

## 数据字段
- 发布时间：时间序列分析
- 岗位名称：职位分类、关键词提取
- 工作城市：地域分布分析
- 薪资水平：薪资预测、影响因素分析
- 学历要求：教育背景与薪资关系
- 经验要求：经验与薪资关系
- 岗位描述：技能提取、要求分析（核心NLP字段）
- 公司名称：企业分析
- 公司规模：规模与薪资关系
- 公司行业：行业趋势分析

## 编码规范

### Python代码风格
- 使用Python 3.8+特性
- 遵循PEP 8规范
- 函数和类必须有详细的中文docstring
- 变量命名使用有意义的英文，注释使用中文
- 每个模块顶部说明功能和作者信息

### 数据处理原则
- **分块处理**：5GB数据必须使用chunk方式读取，避免内存溢出
- **增量处理**：处理结果及时保存，支持断点续传
- **并行计算**：使用multiprocessing或dask进行并行处理
- **缓存机制**：中间结果缓存到磁盘，避免重复计算

### NLP处理规范
- 使用jieba进行中文分词
- 停用词过滤必须包含招聘领域特定词汇
- 文本预处理：去除HTML标签、特殊字符、统一格式
- 模型选择：优先使用轻量级模型（如TF-IDF、Word2Vec），大模型仅用于关键任务

### 性能优化
- 数据读取使用pandas的chunksize参数
- 文本处理使用向量化操作，避免循环
- 大规模计算使用dask或vaex替代pandas
- 模型训练使用GPU加速（如果可用）

### 项目结构
```
Employ26/
├── data/                    # 原始数据（不提交到git）
├── output/                  # 输出结果
│   ├── split_out/          # 分割后的数据
│   ├── processed/          # 预处理后的数据
│   ├── models/             # 训练的模型
│   └── reports/            # 分析报告
├── src/                     # 源代码
│   ├── preprocessing/      # 数据预处理
│   ├── nlp_analysis/       # NLP分析模块
│   ├── visualization/      # 可视化模块
│   └── utils/              # 工具函数
├── notebooks/              # Jupyter notebooks
├── tests/                  # 单元测试
└── docs/                   # 文档
```

## 关键技术栈
- **数据处理**：pandas, dask, vaex
- **NLP**：jieba, gensim, transformers, sklearn
- **可视化**：matplotlib, seaborn, plotly, pyecharts
- **机器学习**：scikit-learn, xgboost, lightgbm
- **深度学习**：pytorch（可选，用于高级任务）

## 注意事项
1. 所有数据文件不提交到git，使用.gitignore排除
2. 代码必须包含异常处理和日志记录
3. 每个分析步骤输出进度信息
4. 结果必须可复现，设置随机种子
5. 内存使用监控，及时释放不用的变量

## AI辅助开发指南
- 生成代码时考虑5GB数据量的处理效率
- 优先提供分块处理和并行计算方案
- 提供完整的错误处理和日志记录
- 代码注释详细，便于理解和维护
- 提供使用示例和测试用例

## 技能抽取专项规则

本项目中的"技能抽取"必须遵守以下原则：

### 1. 技能定义
- **技能不等于"高频词"或"名词短语"**
- 抽取目标是"可标准化、可统计、可用于月度/城市/行业趋势分析的职业技能"
- 技能必须满足：可传授性、可评估性、工具性、具体性

### 2. 默认排除内容
除非用户明确要求纳入，否则必须排除：
- **软素质词**：责任心、抗压能力、沟通能力、团队精神、执行力等
- **空泛职责词**：负责、执行、推进、管理、协作、配合、跟进等
- **泛岗位动作词**：开发、运营、销售、分析等单独出现时（无具体技术栈）
- **福利待遇词**：五险一金、餐补、年终奖、带薪年假等
- **工作条件词**：加班、出差、驻外、轮班等
- **人格特质词**：认真、细心、耐心、热情、积极等
- **岗位名称词**：产品经理、项目经理、工程师、经理、主管等### 3. 必备组件
任何技能抽取方案都必须包含：
- **技能定义**：明确什么是技能，什么不是
- **技能分类体系**：如编程语言、框架、工具、证书等（至少3-6大类）
- **负样本/黑名单策略**：明确列出要排除的词汇类型和具体词汇
- **别名归一化策略**：处理Python/python/PYTHON、JS/JavaScript等变体
- **评估方案**：如何量化评估精度、召回率、噪声率

### 4. 质量优先原则
- **优先高精度和低噪音**，不要盲目追求高召回
- 对于趋势分析任务，85%精度 + 65%召回 > 60%精度 + 85%召回
- 噪声会严重扭曲统计结果，必须严格控制

### 5. 开发流程
- **先输出方案与理由，再动代码**
- 方案必须包含：问题定义、方法选择、预期效果、评估方法
- 如果现有方法建立在错误前提上，应优先重构问题定义，而不是微调参数

### 6. 词典管理规范
- 所有技能词典、黑名单、同义词表必须存储在 `dicts/` 目录
- 不允许在代码中硬编码词汇列表
- 词典文件必须使用UTF-8编码，支持注释（#开头）
- 修改词典后无需修改代码，重新运行即可生效

### 7. 技能抽取方法选择
- **禁止**：单纯使用TF-IDF提取高频词作为技能
- **禁止**：无约束的Word2Vec多轮扩展（容易语义漂移）
- **推荐**：种子词典 + 精确匹配 + 多层黑名单过滤
- **可选**：受控的扩展（1轮，高阈值0.75+，严格过滤）

### 8. 评估要求
- 必须提供量化评估指标（精度、召回、F1、噪声率）
- 必须进行人工抽样检查（至少100个样本）
- 必须提供分类统计（各类技能的数量分布）
- 必须识别和报告低置信度技能（供人工审核）

## 文件修改规范
- 禁止通过生成并运行辅助Python脚本来修改其他文件
- 修改文件只允许使用 Write（直接覆盖）和 StrReplace（精确替换）
- 文件超过字符限制时，用两次Write或Write+StrReplace组合完成
- 语法错误时用Read定位问题行，用StrReplace直接修复